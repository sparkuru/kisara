# Trellis Upstream Provenance

The initialized Trellis workflow uses templates from the installed npm package
`@mindfoldhq/trellis` version `0.6.17`, authored by Mindfold LLC. Its package
metadata identifies `AGPL-3.0-only` and the upstream repository
<https://github.com/mindfold-ai/trellis>. The exact license text from that
installed distribution is preserved verbatim in [LICENSE](LICENSE).

The copied framework files include `.trellis/scripts/`, `.trellis/agents/`,
`.trellis/workflow.md`, `.trellis/config.yaml`, `.trellis/.gitignore`, the
initial thinking guides, the managed block in `AGENTS.md`, and `.gitattributes`.
Existing upstream file notices are retained. No separate author copyright
notice or NOTICE file was found in the installed package; none is fabricated.
Version and template-hash metadata record the installed template baseline.

On 2026-09-26, Kisara added project-owned policies in
`.trellis/spec/trellis-plus/`. Bootstrap completion on 2026-09-27 populated
`.trellis/spec/backend/` from application source and updated task/journal
records. Generated executable framework files were not modified.

This notice documents the copied framework's provenance. It does not assign a
license to Kisara application code or replace the notices for its bundled data.
Personal `.agents/` and `.codex/` installations, session identity/pointers, and
runtime/cache state are excluded from these commits.
